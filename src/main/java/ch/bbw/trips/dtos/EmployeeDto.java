package ch.bbw.trips.dtos;

public record EmployeeDto(
         Long id,
         String name,
         String jobTitle,
         String username,
         String password,
         String token
) {
}
