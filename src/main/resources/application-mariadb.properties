# Activate with --spring.profiles.active=mariadb (or SPRING_PROFILES_ACTIVE=mariadb).
# Matches the credentials in docker/docker-compose.yml; start that stack first with:
#   docker compose -f docker/docker-compose.yml up -d
spring.datasource.url=jdbc:mariadb://localhost:3306/db_biztrips
spring.datasource.username=root
spring.datasource.password=bbw123
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
